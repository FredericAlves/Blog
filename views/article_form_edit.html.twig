{% extends 'layout.html.twig' %}

{% block title %}Modifier un article{% endblock %}

{% block content %}
    <div class="well">
        <p></p>
    <div class="row">
    <div class="col-sm-offset-5 col-sm-4">
        <a href="/admin"><button type="button" class="btn btn-primary"><span class="glyphicon glyphicon-menu-left"></span>Retour à la liste des articles</button></a>
    </div>
    </div>
        <p></p>

        <form action="/admin/article/add" id="form-article" method="post" onsubmit="return valider()" name="formSaisie">
            <div class="row">
                <input name="id" id=$id  type="hidden" value="{{ article.id }}">
                <div class="form-group">
                    <div class="col-sm-6">
                    <label for="title">Titre</label>
                    <input value ="{{ article.title }}" type="text" name="title" id=$title class="validate" data-error="wrong" data-success="right" required/>
                        <p></p>
                </div>
            </div>



                <div class="form-group">
                <div class="col-sm-12">
                    <zonetinymce type="textarea" name="content" id=$content  >{{ article.content | raw}}</zonetinymce>
                </div>
            </div>

                <div class="form-group">
                    <div class="col-sm-offset-5 col-sm-4">
                        <p></p>
                <input type="submit" class="btn btn-primary" value="Modifier l'article" formnovalidate="true"/>
                    </div>
            </div>
            </div>
        </form>
    </div>

{% endblock %}